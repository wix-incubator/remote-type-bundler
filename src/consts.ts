export enum CDN {
  UNPKG = 'unpkg',
  JSDELIVR = 'jsdelivr'
}

export const UNPKG_BASE = 'https://unpkg.com/';
export const JSDELIVR_BASE = 'https://cdn.jsdelivr.net/npm/';
export const JSDELIVR_API_BASE = 'https://data.jsdelivr.com/v1/package/npm/';
